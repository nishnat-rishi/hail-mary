-- new library for organic interactions with gestures
-- clamping objects to random stuff and so on depending on
-- active touches

-- uses gestures as input on every frame as opposed to
-- dt with anim:update(dt)

local spring = {

}

return spring